create table auth_user (
    id bigint generated by default as identity primary key,
    username varchar(80) not null unique,
    password_hash varchar(120) not null,
    full_name varchar(120) not null,
    active boolean not null default true,
    roles varchar(120) not null
);

create table t95_usu03 (
    usr_siglo21 varchar(8) primary key,
    usr_nombre varchar(43) not null,
    usr_status char(1) not null default 'N',
    usr_cid varchar(10),
    usr_offset varchar(8),
    usr_cargo varchar(18),
    usr_empresa varchar(4) not null,
    usr_centro varchar(4) not null,
    usr_pers varchar(8) not null,
    usr_dominio varchar(4),
    usr_nodo varchar(4),
    usr_autoriza char(1) not null default '0',
    usr_updfch varchar(8),
    usr_updtime varchar(6),
    usr_updusr varchar(8),
    usr_updterm varchar(4),
    usr_fchlogon varchar(8),
    usr_timelogon varchar(6),
    usr_token varchar(8),
    usr_historia varchar(80),
    usr_fchnewpass varchar(8),
    usr_fchcambio varchar(8),
    usr_oficina_swift varchar(20)
);

create table t95_ban04 (
    usr_siglo21 varchar(8) primary key,
    usuario_bancs varchar(8),
    terminal_bancs varchar(5),
    estado_proceso varchar(2) not null default 'AC'
);

create table t95_obs04 (
    usr_siglo21 varchar(8) primary key,
    observacion varchar(80) not null
);

create table t95_par02 (
    id bigint generated by default as identity primary key,
    cod_grupo_usuario varchar(8) not null,
    cod_parametro varchar(8) not null,
    descripcion varchar(80) not null
);

create table t01_gre20 (
    perfil varchar(8) primary key,
    descripcion varchar(80) not null
);

create table t06_tc005 (
    empresa varchar(4) primary key,
    descripcion varchar(80) not null
);

create table t06_tc007 (
    empresa varchar(4) not null,
    centro varchar(4) not null,
    descripcion varchar(80) not null,
    primary key (empresa, centro)
);

create table t95_log01 (
    id bigint generated by default as identity primary key,
    transaccion varchar(40) not null,
    operacion varchar(20) not null,
    detalle varchar(4000) not null,
    before_data varchar(4000),
    after_data varchar(4000),
    usuario varchar(80) not null,
    created_at timestamp not null default current_timestamp
);
