# The environment mode for the admin UI.
NEXT_PUBLIC_ENVIRONMENT=dev
# Same-origin default: /nexti-mark.svg. Point to /logo-nexti.png when you add the official asset to public/.
# NEXT_PUBLIC_LOGO_PATH=/logo-nexti.png

# Local dev navigation helper.
NEXT_PUBLIC_CHAT_URL=http://localhost:3000/chat
