version: 1
project_name: "testing"
model: gpt-5.4-mini
context: |
  Use `openspec/context.md` as the persistent project context.
  Use `user-stories.md` as the source of truth for the user story block.
  Keep the active change under `openspec/changes/cmncqck580002ifzg0q4id28x`.
guardrails:
  - Keep the scope anchored to the provided user stories.
  - Prefer small, testable deltas.
  - Keep OpenCode and OpenSpec configuration separate.
  - Do not change the target stack.
